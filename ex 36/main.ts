// Make a function And  Call the function.

function make_shirt (size:string, printMessage: string){
     console.log(` You selected ${size} size shirt with ${printMessage} print on shirt`)
}

make_shirt("Large","Code with maya")

